// Available to customize the build
task wrapper(type: Wrapper) {
  jarFile = rootProject.file('.gradle-wrapper/gradle-wrapper.jar')
}

project(':bnd') {
  task refreshPde(dependsOn: ':pde:buildPde') {
  }
}